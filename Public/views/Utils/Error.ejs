<!DOCTYPE html>

<html>
    <head>
        <title>Erreur</title>
    </head>

    <body>
        <div class="container">
            <h1>Une erreur est survenue</h1>
            <% if (title) { %>
                <h3><%= title %></h3>
            <% } %>
            <p><%= message %></p>	
        </div>
    </body>
</html>